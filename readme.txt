to run the script though the command line:

1. activate a virtual environment with beautiful-soup installed as a dependency
2. type:   python index.py <url>
3. should receive a json obj with the og meta properties OR an object specifying the error in case you provided a url is not valid
